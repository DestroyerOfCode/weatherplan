spring:
  data:
    mongodb:
      port: 27018
      username: "user"
      password: "pass"
      host: "localhost"
      database: "weather"
      authentication-database: "admin"
server:
  port: 8080
weather:
  home-weather:
    schema: "http"
    sub-domain: "localhost"
