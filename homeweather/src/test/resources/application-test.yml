server:
  port: ${SERVER_PORT:8082}
  address: ${SERVER_ADDRESS:localhost}
weather:
  home-weather:
    schema: "http"
    sub-domain: "localhost"
spring:
  application:
    name: "homeweather-test"
gateway:
  port: 8083
